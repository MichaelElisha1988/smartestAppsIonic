export interface Header {
  gtransIcon: string;
  accIcon: string;
  logoIcon: string;
  profile: profile;
}
export interface profile {
  settings: settings;
  userName: string;
  profileIcon: string;
}

export interface settings {}
